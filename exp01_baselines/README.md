# Baselines

Paper reference models using CIFAR-100 augmentation and `zero_init_residual=False`:

- CIFAR-style ResNet-18.
- CIFAR-style ResNet-34.

ResAttn and older zero-initialized runs were removed because they are not part of the final paper experiments.
